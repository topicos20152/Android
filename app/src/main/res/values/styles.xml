<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MaterialDrawerTheme.Light.DarkToolbar.TranslucentStatus">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="material_drawer_background">@color/colorTextButton</item>
        <item name="material_drawer_primary_text">@color/colorPrimary</item>
        <item name="material_drawer_hint_text">@color/colorSecondary</item>
        <item name="material_drawer_divider">@color/colorPrimary</item>
        <item name="material_drawer_selected">@color/colorSecondary</item>
        <item name="material_drawer_selected_text">@color/colorPrimary</item>
        <item name="material_drawer_header_selection_text">@color/colorSecondary</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- ActionBar vs Toolbar. -->
    <style name="Widget.MyApp.ActionBar.Solid" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="background">@color/colorPrimary</item> <!-- No prefix. -->
        <item name="elevation">4dp</item> <!-- No prefix. -->
        <!--<item name="titleTextStyle">...</item> &lt;!&ndash; Style vs appearance. &ndash;&gt;-->
    </style>

    <style name="ThemeOverlay.MyApp.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Parent theme sets colorControlNormal to textColorPrimary. -->
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item>
    </style>

    <style name="Theme.MyApp" parent="Theme.AppCompat">
        <item name="actionBarStyle">@style/Widget.MyApp.ActionBar.Solid</item>
        <item name="actionBarTheme">@style/ThemeOverlay.MyApp.ActionBar</item>
        <item name="actionBarPopupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>
</resources>
